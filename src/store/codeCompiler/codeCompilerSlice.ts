import codeCompilerPreviewSlice from "./actions/codeCompilerPreviewSlice";
import codeCompilerSettingSlice from "./actions/codeCompilerSettingSlice";

const codeCompilerSlice = {
  codeCompilerPreviewSlice,
  codeCompilerSettingSlice,
};

export default codeCompilerSlice;
